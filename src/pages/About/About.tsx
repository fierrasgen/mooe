import React, { FC } from 'react';
import { Header } from '../../features/Header';

export const About: FC = () => {
    return (
        <div>
            <Header />
            ABOUT
        </div>
    );
};
