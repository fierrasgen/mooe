import { FC } from 'react';
import { Header } from '../../features/Header';

export const Contacts: FC = () => {
    return (
        <div>
            <Header />
            Contacts
        </div>
    );
};
