import styled from 'styled-components';

export const IconLink = styled.a`
    width: 24px;
    height: 24px;
    color: #D9DADA;
    transition: color 0.2s ease-in;

    &:hover {
        color: #000000;
    }
`;
